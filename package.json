{
  "name": "lablens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Medication-aware lab triage agent for the Agents Assemble Healthcare AI Hackathon",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
